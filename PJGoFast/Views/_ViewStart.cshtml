@{
    var controller = ViewContext.RouteData.Values["controller"]?.ToString();
    Layout = string.Equals(controller, "Home", StringComparison.OrdinalIgnoreCase)
        ? "_LayoutMobile"
        : "_Layout";
}
